# Vendored plugin test runtime

These runtime files come from `@bb/plugin-sdk` 0.4.1 and are used only by
Vitest. BB supplies the real runtime when the plugin is loaded. Refresh these
files together with `../types/` whenever `engines.bbPluginSdk` changes.
