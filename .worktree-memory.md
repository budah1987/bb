# Worktree Memory — bbamir/main

Short-lived operational state for this checkout. Not durable project knowledge.

## Goal

Implement the approved BBamir performance and durability plan through merged
PRs, rebuild the packaged app, and stress test until measured targets pass.

## Baseline

- Packaged renderer reached 5.4 GB RSS within four minutes.
- Renderer CPU spiked to 70–78%.
- Renderer peak memory reached 9.3 GB.
- Timeline builds blocked the server event loop for 150–560 ms.
- Git worker remained near 0% CPU, so Git is not the current primary cause.

## Current status

- Branch `bbamir/main` contains merged PRs #37, #38, and #40–#55.
- Final package is installed at `/Applications/BBamir.app` and is running.
- The renderer DOM root cause was a collapsed Git panel with 43,854 hidden rows.
- PR #53 unmounts collapsed content and virtualizes large changed-file lists.
- PRs #54–#55 add the isolated packaged performance release driver.

## Required targets

- Renderer stays below 1.5 GB after a 30-minute stress run.
- Renderer peak stays below 2.0 GB.
- Idle renderer CPU P95 stays below 10%.
- Timeline projection P95 stays below 75 ms.
- Browser pages survive task changes without extra browser instances.

## Next action

No performance action remains. The final 30-minute gate passed with 859 MB peak
RSS, 600 MB final-window RSS, 5.81% idle CPU P95, zero browser attach growth,
and zero post-warm-up memory growth. The final durability, bundle, packaged
smoke, server timeline, browser durability, and installed-route checks passed.
