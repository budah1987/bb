# Worktree handoff

- Goal: Built-in, environment-scoped iOS Simulator for BB, planned with the Model Relay/Fable Plan Council and implemented by Sol.
- Status: Implementation complete and verified. Fable approved the implementation plan with concerns resolved in the final design; two post-implementation resume attempts failed at provider startup with zero tokens consumed.
- Opening UX: Thread right panel → New Tab → Open simulator → choose device → Start simulator. The tab is singleton per environment. Closing it keeps the session running; Stop ends it.
- Architecture: Host daemon owns `xcrun`/`serve-sim`, sessions, cancellable attach, controls, and the authenticated loopback MJPEG gateway. Server maps it through existing host RPC/shared-port tunneling. UI, public server contract, SDK, CLI, and guide are all implemented.
- Verification: app 2,495 tests; SDK 90 tests; CLI 376 tests; bb-app 48 tests; host/server contract tests; relevant typechecks and lint; production bb-app build; packed-tarball smoke. Lint has 147 pre-existing app warnings and no errors. `git diff --check` passes.
- Live simulator limitation: this machine does not have `simctl` available, so the actual Xcode Simulator could not be booted here; the unsupported-host state is exercised by the implementation.
- Model Relay: workflow `workflow-mskj6kl8-2bc235`, Fable task `relay-mskj8ydk-497005`, approved plan in `.context/model-relay/relay-mskj8ydk-497005/run-2.result.md`.
- Dependency: `serve-sim@0.1.45` (Apache-2.0) in host daemon and distributable bb-app.
